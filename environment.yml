name: spatialproteomics-threshold-tuner
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  - numpy
  - pandas
  - matplotlib
  - xarray
  - pyyaml
  - streamlit>=1.30
  - pip:
      - spatialproteomics>=0.8.0
